# Plan v0.1.9 - Review & Cleanup Release

**Fractal**: Lemon - a convergence-based Newton-type fractal with configurable denominator power

This is a review/cleanup release. No new features -- focused on dependency updates, documentation quality, repository hygiene, and preparing the codebase for the v0.2.x feature series.

---

## COMPLETED

### [x] Bug Fix: Executables banner in README
The non-programmer banner in README.md was being co-opted with version news.
- Stripped to just: `> **Non-programmers:** Pre-built Windows executables available in [builds/](builds/)`
- Removed version badge (kept going stale)
- Removed "New in v0.1.7" line
- Removed hardcoded version from Quick Start download instructions
- Removed emoji from License header

### [x] Update dependencies
- Ran `cargo update` to update all locked dependencies
- Ran `cargo upgrade --compatible` to bump Cargo.toml version constraints:
  - rayon: 1.8 -> 1.11
  - once_cell: 1.19 -> 1.21
  - clap: 4.4 -> 4.5
  - tempfile: 3.8 -> 3.25
- scala-chromatica updated to latest commit
- All 54 tests passing, `cargo check` clean
- **Incompatible upgrades available but NOT applied** (need testing/migration):
  - eframe: 0.25 -> 0.33 (major egui ecosystem change - defer to its own task)
  - directories: 5.0 -> 6.0
  - image: 0.24 -> 0.25
  - png: 0.17 -> 0.18
  - rfd: 0.12 -> 0.17
  - indicatif: 0.17 -> 0.18

### [x] Fix pre-existing test failure
- `test_tetration_default_view` expected zoom=0.5 but code was changed to 0.45 in v0.1.8
- Updated test assertion to match

### [x] Review future plans
- Added "Fractal & Colormap: TBD" note to v0.2.4 and v0.3.0 (were missing it)
- Formalized the GPU-per-fractal inline note in v0.2.0 (was a TODO comment, now proper strategy text)
- Added GPU phasing note to v0.2.1 (Julia Set as next GPU candidate)
- Added version-staleness note to v0.2.0 dependency section

### [x] Code size audit
- No source files over 3000 lines. Largest is export.rs at 1179 lines.
- No action needed; file sizes are reasonable.

---

## COMPLETED TASKS

All tasks completed.

---

### [x] Task 1: Remove emojis from documentation files

**Context**: AGENTS.md forbids emojis in all documentation. Several files have emoji violations.

**Files to edit**:
1. `CHANGELOG.md` - Lines near 225-234 have checkmark/cross emojis. Replace:
   - `✓` and `❌` -> use `[OK]` and `[FAIL]` or just remove
   - `✅` -> remove or replace with `[PASS]`
2. `BENCHMARKS.md` - Line 45 has `✅`. Replace with `[PASS]` or remove.
3. `COLORMAP_SAVELOAD.md` - Lines 26, 30, 32, 38, 39 reference GUI buttons that use emojis. These are **accurate descriptions of actual button labels** in the GUI code, so they can stay. But verify: if the GUI button labels can be quoted in a non-emoji way (e.g., "the Save/Load ColorMap section"), do that instead.

**Validation**: Search all .md files for common emojis after editing. CHANGELOG and BENCHMARKS should have none.

---

### [x] Task 2: Cleanup agents.md

**Context**: AGENTS.md should be streamlined for LLM effectiveness. Currently 158 lines.

**Changes to make**:
1. Fix typo in overview: "caried" -> "carried"
2. The "Adding New Fractals" checklist (lines 56-131) is thorough and should stay as-is.
3. The "Architecture Notes" section should mention the FractalGUI trait (added in v0.1.8) -- currently only mentions grouped state pattern.
4. The "Final to-do before each release" section could add a note: "Run `cargo test --lib` to verify all tests pass before release."
5. Consider adding a "Dependency Management" section:
   ```
   ## Dependency Management
   - Safe updates: `cargo upgrade --compatible` (bumps within semver range)
   - Check available: `cargo upgrade --dry-run --incompatible` (shows breaking changes)
   - Lock file update: `cargo update` (updates Cargo.lock to latest within constraints)
   - Always run `cargo check` and `cargo test --lib` after updating.
   ```
6. Leave overall structure intact -- it works well.

**Validation**: Read through result, confirm it's under 180 lines and reads clearly.

---

### [x] Task 3: Restructure img_resources

**Context**: The img_resources/ directory is flat with 11 images. It should be organized into subdirectories.

**Step 1 - Create directories**:
```
img_resources/banners/    - ultrawide images (4320x1080), used as section dividers
img_resources/gallery/    - visible gallery images (3840x2160), shown without clicking
img_resources/gallery_expanded/  - additional images shown in "View more" dropdown
```

**Step 2 - Move files**:

Banners (4320x1080, ultrawide):
- `julia_set_4320x1080_4xLanczos31771181678.png` -> `banners/`
- `mandelbrot_4320x1080_4xLanczos31771181432.png` -> `banners/`
- `burning_ship_4320x1080_4xLanczos31771182212.png` -> `banners/`
- `cactus_4320x1080_4xLanczos31771181941.png` -> `banners/`

Gallery (3840x2160, main gallery table):
- `mandelbrot_3840x2160_4xLanczos31768372087.png` -> `gallery/`
- `julia_set_3840_4xLanczos31768634818.png` -> `gallery/`
- `tetration_3840x2160_4xLanczos31771201252.png` -> `gallery/`
- `mandelbrot_3840x2160_4xLanczos31768802455.png` -> `gallery/`

Gallery Expanded (in the "View more" dropdown):
- `multifractal-julia_3840x2160_4xLanczos31769398723.png` -> `gallery_expanded/`
- `mandelbrot_3840x2160_4xLanczos31768887944.png` -> `gallery_expanded/`
- `mandelbrot_3840x2160_4xLanczos31769238284.png` -> `gallery_expanded/`

**Step 3 - Update README.md image paths**:
Every `img_resources/filename.png` reference must be updated to `img_resources/banners/filename.png`, `img_resources/gallery/filename.png`, or `img_resources/gallery_expanded/filename.png` respectively.

**Step 4 - Update .gitignore**:
The existing `!img_resources/*.png` rule won't match subdirectories. Change to `!img_resources/**/*.png`.

**Validation**: All images render correctly in README.md on GitHub after push.

---

### [x] Task 4: Improve banner placement in README

**Context**: Currently 4 banners are clustered in the first half of README.md (before "Quick Start") and none appear in later sections. The goal is more even distribution.

**Current layout**:
- Hero banner (Julia) - top of page -- KEEP
- Mandelbrot banner - before "Key Features" -- KEEP
- Burning Ship banner - between feature subsections -- KEEP
- Cactus banner - before "Quick Start" separator -- MOVE

**Suggested change**: Move the Cactus banner from before "Quick Start" to before "Usage Guide" (around line 131). This spaces the banners more evenly:
1. Julia hero -> top
2. Mandelbrot -> Key Features
3. Burning Ship -> mid-features
4. Cactus -> Usage Guide

The README currently has a `---` separator before "Quick Start" at line 46. Remove the Cactus banner from there and place it before `## Usage Guide`.

**Validation**: Review README in a Markdown previewer. Banners should feel evenly spaced, not clustered.

**NOTE**: This task should be done AFTER Task 3 (img_resources restructure), since file paths will change.

---

### [x] Task 5: Markdown inventory review

**Context**: The project has 7 root-level markdown files. Assessment:

| File | Lines | Verdict |
|------|-------|---------|
| README.md | 259 | Fine - primary project document |
| CHANGELOG.md | 425 | Fine - grows with each release |
| AGENTS.md | 158 | Fine - LLM instructions |
| COLORMAP_SAVELOAD.md | 267 | Review - possibly verbose for its purpose |
| BENCHMARKS.md | 210 | Fine - reference data |
| METADATA_FORMAT.md | 187 | Fine - technical spec |
| THIRD_PARTY_LICENSES.md | 159 | Fine - required legal document |

**Action**: Review COLORMAP_SAVELOAD.md for verbosity. It's a user-facing doc for the colormap save/load feature. If it has excessive implementation detail that users don't need, trim it. Keep the "how to use" parts, trim internal architecture details unless they're useful for contributors.

**Validation**: No markdown file should feel padded or redundant. Each should serve a clear purpose.

---

### [x] Task 6: Repository hygiene check

**Context**: The .gitignore ignores `plans/` and `AGENTS.md` (these are LLM-workspace files, not for public repo). Verify consistency.

**Action items**:
1. Verify `builds/` is NOT in .gitignore (binary releases are distributed there)
2. Verify `captures/` IS in .gitignore (test/debug captures)
3. The `test_load.rs` file at root level looks like a leftover test file. Investigate whether it should be deleted or moved to `tests/`.
4. Check if there are any other stray files at root that should be cleaned up.
5. Run `git status` and verify no untracked files that should be tracked, or tracked files that should be ignored.

**Validation**: Clean `git status` output with no surprises.

---

### [x] Task 7: Incompatible dependency upgrade investigation

**Context**: Several dependencies have breaking version upgrades available (identified in dependency update). These should NOT be applied blindly but investigated for migration effort.

**Upgrades to investigate** (in order of risk/impact):
1. **eframe 0.25 -> 0.33**: HIGHEST RISK. 8 minor versions of the egui ecosystem. Will require significant GUI code changes. Should probably be its own plan item or deferred to a future release.
2. **image 0.24 -> 0.25**: Medium risk. Check changelog for breaking API changes in export.rs.
3. **png 0.17 -> 0.18**: Medium risk. Used in export.rs for metadata writing.
4. **rfd 0.12 -> 0.17**: Low-medium risk. File dialog library.
5. **directories 5.0 -> 6.0**: Low risk. Config directory paths.
6. **indicatif 0.17 -> 0.18**: Low risk. CLI progress bars.

**Action**: For each, check the crate's CHANGELOG/migration guide. Write a brief note in this plan about estimated effort. Do NOT apply the upgrades yet.

**Recommendation**: Apply items 4-6 (low risk) in this release if migration is trivial. Defer items 1-3 to a future release.

**INVESTIGATION RESULTS**:

| Dependency | Version Change | Files Affected | Risk | v0.1.9 Decision |
|------------|---------------|----------------|------|-----------------|
| eframe | 0.25 -> 0.33 | main.rs, gui.rs, colorschemes_gui.rs, fractal_gui.rs, tetration.rs | **HIGH** | DEFER |
| image | 0.24 -> 0.25 | filtering.rs | Medium | DEFER |
| png | 0.17 -> 0.18 | export.rs | Medium | DEFER |
| rfd | 0.12 -> 0.17 | gui.rs, colorschemes_gui.rs | Low | **UPGRADE** |
| directories | 5.0 -> 6.0 | (unused) | Trivial | **REMOVE** |
| indicatif | 0.17 -> 0.18 | cli.rs | Low | **UPGRADE** |

**Applied changes:**
- Upgraded rfd 0.12 -> 0.17
- Upgraded indicatif 0.17 -> 0.18
- Removed `directories` dependency (unused - config dirs handled by scala-chromatica)

---

### [x] Task 8: Fractal of the release - LEMON

**Implementation Complete:**
- Created `src/fractals/lemon.rs` with full Fractal trait implementation
- Formula: `z_{n+1} = z_0 * z_n^2 * (z_n^2 + 1) / (z_n^2 - 1)`
- **Convergence-based** iteration (stops when |z_{n+1} - z_n| < threshold)
- Parameter: `convergence_exp` - exponent for threshold (10^-exp notation)
- Added to FractalType enum, all() list, create_instance(), render_gui(), reset_view_and_params()
- Added `lemon_convergence_exp` to InputState with parser
- 7 unit tests passing
- Updated v0.2.8 plan created for Lemon GPU implementation

---

## Plan Review Notes

### Future plan assessment summary

| Plan | Status | Notes |
|------|--------|-------|
| v0.2.0 (GPU) | Good | Thorough. Formalized GPU-per-fractal strategy. Version numbers are illustrative. |
| v0.2.1 (GIF) | Good | Short but appropriate for planning stage. Added GPU phasing note. |
| v0.2.2 (Precision) | Good | Research phase, appropriate detail level. |
| v0.2.3 (Library) | Very detailed | 746 lines. Dependency versions will be stale by implementation time. Could potentially be reordered before GPU work for cleaner architecture, but current ordering is acceptable. |
| v0.2.4 (SEO/Cleanup) | Sparse | Just a placeholder. Will flesh out as v0.2.x progresses. Added fractal TBD note. |
| v0.3.0 (Job Queue) | Rough outline | Appropriate for distant-future planning. Added fractal TBD note. |

### Observations for future LLMs
- The eframe 0.25->0.33 upgrade is the elephant in the room. It touches gui.rs, colorschemes_gui.rs, main.rs, and app_state.rs. Consider whether to do it standalone or bundle with v0.2.0 GPU work.
- v0.2.3 (Library Decoupling) has a logical argument for coming before v0.2.0 (GPU), since clean library boundaries would make GPU integration easier. However, the current ordering works and changing it would require re-examining all plan dependencies.
- v0.2.8 added for Lemon GPU implementation (9th fractal)

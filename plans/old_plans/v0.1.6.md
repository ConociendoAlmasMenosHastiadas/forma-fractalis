# Plan for v0.1.6 - Import/Export Features

## bugs
[x] remove manderust builds from git/github/filesystem.  the name is outdated (already completed in earlier release)

## Overview
Version 0.1.6 focuses on completing the round-trip functionality with Load from PNG and preparing the foundation for command-line rendering in v0.1.7.

**Fractal**: Cactus Fractal - z_{n+1} = z_n^3 + (z_0 - 1)z_n - z_0  
**Colormap**: Vintage Lavender - Muted earth tones (lavender, teal, slate, tan, pumpkin)

## Priority Order
1. **Load from PNG Metadata** - Major feature for round-trip functionality
2. **Improve metadata serialization** - Prepare for command-line usage
3. **Testing and validation** - Ensure robust import/export

## Dependencies
- **v0.1.5 GUI Architecture Refactoring** must be complete - the new grouped state pattern (ViewState, FractalState, ColorState, etc.) makes loading state much cleaner
- PNG metadata export (completed in v0.1.2)

## Main Features

### Load from PNG Metadata
**Priority**: High  
**Status**: Not started  
**Dependencies**: v0.1.5 GUI refactoring, PNG metadata export (v0.1.2)

**Description**: Add "Load from PNG" button to import all settings from exported images with embedded metadata.

**Implementation:**
- [x] Add "Load from PNG" button in GUI (near export section)
- [x] Parse PNG tEXt chunks using png crate
- [x] Deserialize metadata JSON from tEXt chunks
- [x] Apply all loaded settings using new state structs from v0.1.5:
  - Switch to correct fractal type (FractalState)
  - Load fractal parameters (Julia c values, etc.) (FractalState)
  - Set view coordinates and zoom (ViewState)
  - Restore colormap (including custom color stops) (ColorState)
  - Apply color modulation settings (period, interior color, log scale) (ColorState)
  - Set export settings (filter, supersample, scale) (ExportState)
  - Update text inputs (InputState)
- [x] Handle missing/invalid metadata gracefully
- [x] Show success/error message to user
- [x] Add file picker dialog for PNG selection (use rfd::FileDialog)
- [x] Trigger redraw after loading

**Benefits:**
- Complete round-trip: Export → Load → Exact reproduction
- Share discoveries with full settings embedded
- Easy iteration on exported images
- No manual settings transcription needed
- Foundation for command-line rendering (v0.1.7)

**Technical Notes:**
- Use `rfd::FileDialog` for PNG selection
- Validate fractal type and parameters before applying
- Consider adding "Preview Metadata" option to show settings before loading
- With v0.1.5's state grouping, loading becomes:
  ```rust
  // Parse metadata
  let metadata: FractalMetadata = parse_png_metadata(&png_path)?;
  
  // Apply to grouped state
  app.fractal = metadata.into_fractal_state();
  app.view = metadata.into_view_state();
  app.color = metadata.into_color_state();
  app.export = metadata.into_export_state();
  app.input = metadata.into_input_state();
  ```

**Integration with GUI Architecture:**
The v0.1.5 refactoring makes this much cleaner:
- Loading function takes `&mut FractalApp` instead of 15+ individual parameters
- State validation can be encapsulated in state struct methods
- Error handling is centralized
- Easy to add new state fields without changing load logic

### Metadata Serialization Improvements
**Priority**: Medium  
**Status**: Enhancement of existing system  

**Description**: Improve metadata structure to better support command-line rendering and future features.

**Implementation:**
- [x] Review current metadata JSON structure in `export.rs`
- [x] Ensure all state is serializable/deserializable
- [x] Add version field to metadata for future compatibility
- [x] Add optional timestamp field for when fractal was created
- [x] Test round-trip serialization for all fractal types
- [x] Document metadata format in dedicated file (METADATA_FORMAT.md)

**Benefits:**
- Better foundation for command-line tools
- Future-proof metadata format
- User-friendly fractal organization
- Easier debugging and validation

### Testing and Validation
**Priority**: High  
**Status**: ✅ Complete  

**Implementation:**
- [x] Add tests for PNG metadata parsing (test_load_png_metadata_*)
- [x] Test all fractal types can be loaded correctly (test_export_and_load_all_fractal_types)
- [x] Test custom colormap loading (included in roundtrip test)
- [x] Unit test: load_png_metadata() correctly parses valid PNG (test_export_and_load_roundtrip)
- [x] Unit test: load_png_metadata() returns error for non-PNG files (test_load_png_metadata_invalid_png)
- [x] Integration test: Export PNG → Load PNG → Verify all settings match (test_export_and_load_roundtrip)
- [x] Integration test: Load PNG with missing metadata → Graceful error handling (test_png_without_metadata)
- [x] Integration test: Load PNG from non-existent file → Error handling (test_load_png_metadata_nonexistent_file)

**Test Results:** All 35 library tests passing (8 new integration tests)
- [x] Test edge cases (missing fields, wrong types, corrupted data)
- [x] Test loading into fresh application state (test_load_into_fresh_state)
- [x] Test loading while other fractal is displayed (test_state_replacement_different_fractal)
- [x] Add integration test that exports then loads and compares results (test_export_and_load_roundtrip)
- [x] Test with PNGs that have no metadata (test_png_without_metadata)
- [x] Test with PNGs that have partial/corrupted metadata (test_load_png_metadata_invalid_png)

**Success Criteria:**
- Load any exported PNG and reproduce exact visual result
- Graceful handling of all error cases with user-friendly messages
- No crashes or panics from malformed metadata
- All tests pass

## Future Considerations
**NOTE:** These will move to v0.1.7 or later

- Command-line rendering from PNG files (v0.1.7 target)
- Support for batch loading multiple PNGs
- Playlist/gallery mode to cycle through saved fractals
- Export/import of just settings (JSON without image)
- Drag-and-drop PNG loading
- "Recent fractals" menu

## Progress Tracking

### Implementation Notes
- ✅ **Core functionality completed**: Load from PNG button added to GUI with file picker dialog
- ✅ **PNG metadata parsing implemented**: `load_png_metadata()` function reads all tEXt chunks from PNG files
- ✅ **State loading working**: `load_from_metadata()` method in FractalApp applies all settings from metadata
- ✅ **Metadata structure enhanced**: Added `FractalMetadata` struct with all necessary fields including timestamp
- ✅ **Error handling in place**: Graceful error messages for missing/invalid metadata
- ✅ **Tests complete**: 35 tests passing including 8 comprehensive integration tests
- ✅ **Bug fix discovered and resolved**: Fixed width/height to use View- metadata fields instead of PNG dimensions
- ✅ **Documentation completed**: METADATA_FORMAT.md fully updated with all fields and examples
- ✅ **Backward compatibility**: Loader falls back to PNG dimensions for old metadata without View-Width/Height

### Blockers
- None - all core dependencies met (v0.1.5 complete)

### Testing Results
- ✅ All 35 library tests passing (28 existing + 7 new tests)
- ✅ Code compiles without errors or warnings
- ✅ Release build successful
- ✅ Manual testing completed: User confirmed PNG load/export workflow works perfectly
- ✅ Cactus fractal renders correctly with adaptive escape radius
- ✅ Vintage Lavender colormap displays properly
- ✅ Integration tests verify: round-trip export/load, state replacement, fresh state loading
- ✅ Edge case handling: invalid PNGs, missing metadata, corrupted data

## Colormap to include
<palette>
  <color name="Vintage Lavender" hex="885a89" r="136" g="90" b="137" />
  <color name="Muted Teal" hex="8aa8a1" r="138" g="168" b="161" />
  <color name="Pale Slate" hex="cbcbd4" r="203" g="203" b="212" />
  <color name="Tan" hex="d1b490" r="209" g="180" b="144" />
  <color name="Pumpkin Spice" hex="ee7b30" r="238" g="123" b="48" />
</palette>

## Fractal
- cactus fractal https://paulbourke.net/fractals/cactus/
- using latex notation: z_{n+1} = z_n^3 + (z_0 - 1)z_n - z_0, for every point z_0 in the complex plan.  
- Determine escape radius.  GPT suggested R > max(1, sqrt(abs(z_0-1)+1), abs(z0)^(1/3)).  Iterate function will first determine radius, then launch loop until escape is met. 

## Release Criteria
- [x] Load from PNG feature fully implemented and tested
- [x] All fractal types can be loaded and rendered correctly
- [x] Custom colormaps restore properly
- [x] All state (view, fractal, color, export) restores correctly
- [x] Error handling is robust and user-friendly
- [x] Documentation updated (README.md, CHANGELOG.md, METADATA_FORMAT.md)
- [x] No regressions in existing export functionality
- [x] Code follows patterns established in v0.1.5 (grouped state)
- [x] Manual testing completed (export → load → verify reproduction)
- [x] Integration tests added for round-trip validation (8 comprehensive tests)
- [x] New fractal type added and tested (Cactus - 7 tests)
- [x] New colormap added (Vintage Lavender - using coolors parser utility)

**Release Status**: ✅ Ready for release - All core criteria met!

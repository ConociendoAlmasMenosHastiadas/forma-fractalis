# Plan v0.2.5 - Cactus GPU & Performance Polish

**Status: RELEASED 2026-04-29**

**Fractal**: Adj Prob Julia (mothballed → v0.3.7) / ChaosSymmetry1 attractor (actual fractal of release)

## Overview
Version 0.2.5 continues the GPU rollout with Cactus fractal support and includes performance optimization based on learnings from previous GPU implementations.

## Dependencies
- **v0.2.0-v0.2.4** - GPU foundation and 4 fractal implementations provide patterns to follow
- Library architecture from v0.2.3

## GPU Per-Fractal Phasing

### Cactus GPU Implementation
Per v0.2.0's gradual GPU rollout strategy, this release adds GPU support for **Cactus**.

**Implementation:**
- [x] Create `cactus.wgsl` shader
- [x] Cactus uses per-pixel escape radius computed in-kernel (f32 precision sufficient)
- [x] Add Cactus to GPU-capable fractal list in backend selection
- [x] Test: Compare CPU vs GPU output at characteristic coordinates -- GPU test passed 1/1 (512x512, 256 iter, RTX 3080 Ti)
- [x] Benchmark: Measure speedup vs CPU for Cactus specifically -- HD@256: 2.48x, HD@1024: 3.05x, FHD@1024: 4.84x, FHD@2048: 6.75x; see BENCHMARKS.md

**Technical Notes:**
- Cactus iteration: `z_{n+1} = z_n^3 + (c-1)*z_n - c`
- Standard escape radius approach
- No special escape criteria like Tetration

## Performance Optimization

By v0.2.5, we have 5 GPU-enabled fractals. Time to assess overall performance:

### GPU Pipeline Review
- [x] Profile shader compilation time across all fractals -- `perf_log!("[GPU-INIT] Compiled shader ...")` added per shader and total in `new_async()`; total compile time printed unconditionally at startup
- [x] Pre-compilation at startup -- already implemented; all shaders compiled in `new_async()` before first render
- [x] Measure GPU memory usage patterns -- `perf_log!("[GPU-MEM] ...")` added to `render_fractal()` reporting output/staging buffer sizes per render
- [ ] Optimize uniform buffer updates (batch where possible) -- uniform buffer is 48 bytes; recreating it per render is not a measurable bottleneck at this time; deferred to a future release if profiling shows otherwise

### Auto Backend Selection Refinement
- [~] Add `RenderBackend::Auto` variant with fractal + resolution heuristics -- **deferred to v0.3.6**
      v0.3.6 already owns the Auto mode design (priority order, precision cutoffs, GUI status line).
      The heuristics below belong there, not here:
      - Fractal-specific min-iteration threshold for GPU dispatch overhead
      - Preview size threshold (width*height < 128*128 -> prefer CPU)
      - Real-world benchmark data to make thresholds data-driven
      See `plans/v0.3.6.md` section "Backend Selection Heuristics (carried over from v0.2.5)".

### Benchmark Documentation
- [x] Update BENCHMARKS.md with GPU vs CPU comparisons for all 9 GPU fractals (expanded bench to cover all GPU fractals; see v0.2.5 section in BENCHMARKS.md)
- [x] Document recommended settings for different hardware profiles -- added "Recommended Settings by Hardware Profile" section to BENCHMARKS.md

## Dependency Updates - image & png Crates

**Risk Assessment**: Low-moderate - single minor version bumps

### Upgrade Steps
- [x] Update Cargo.toml: `image = "0.25"`, `png = "0.18"`
- [x] Run `cargo check` and address any compiler errors
  - `png` 0.18: `Decoder::new` now requires `BufRead + Seek` -- wrapped `File` in `BufReader`
  - `png` 0.18: `Compression::Default` variant removed -- replaced with `Compression::default()`
- [x] Review CHANGELOG for breaking changes

### Testing
- [x] Verify `cargo test --lib` passes (165 tests, all ok)
- [x] Verify PNG export works correctly (manual test)
- [x] Verify metadata embedding/reading works (manual test) -- `test_export_and_load_all_fractal_types` expanded to cover all 13 non-mothballed fractal types (was 6)
- [x] Verify all filter types produce expected output -- added Gaussian filter test; all 5 filter tests pass (`cargo test --lib`: 165 passed, 0 failed)
- [x] Test high-resolution exports (4K, 8K) -- both render and save correctly via `render` CLI

## GPU Progress Tracker| Fractal | Version | Shader | Status |
|---------|---------|--------|--------|
| Mandelbrot | v0.2.0 | mandelbrot.wgsl | Planned |
| Julia | v0.2.1 | julia.wgsl | Planned |
| BurningShip | v0.2.2 | burning_ship.wgsl | Planned |
| TippetsMandelbrot | v0.2.3 | tippets_mandelbrot.wgsl | Planned |
| MultifractalJulia | v0.2.4 | multifractal_julia.wgsl | Planned |
| **Cactus** | **v0.2.5** | cactus.wgsl | **Done** |
| MarekDragon | v0.2.6 | marek_dragon.wgsl | Pending |
| Tetration | v0.2.7 | tetration.wgsl | Pending |

## Hi-Precision Implementation
This release adds hi-prec CPU support for **Sin Julia**.

Sin Julia iterates z(n+1) = sin(z_n)*c. BigFloat has a native sin() function taking precision and rounding mode -- no special helpers needed. Supports no extra parameters beyond c_real/c_imag.

- [x] Implement supports_hiprec() -> bool { true } in sin_julia.rs
- [x] Implement iterate_hiprec() translating all f64 arithmetic to BigFloat
- [x] 5 unit tests: interior point, escaping point, f64 agreement on robust points, bit-width smoke, precision agreement
- [x] Update plans/capability_table.md: Sin Julia hi-prec no -> yes (v0.2.5)


## Figure out why windows defender flags as "you shouldn't run"
"Windows Protected your PC..." message.

### Root Cause
This is Windows SmartScreen, not Windows Defender AV. SmartScreen uses "reputation scoring" —
an unsigned binary downloaded from the internet starts with zero reputation. Until many users
have run it without incident, SmartScreen shows the "Windows protected your PC" dialog with
no publisher name shown.

### Options (best → worst tradeoff)

1. **Code signing with an EV certificate** (full fix, expensive)
   - An Extended Validation (EV) code-signing certificate immediately establishes SmartScreen
     reputation regardless of download count.
   - Cost: ~$300–500/year from DigiCert, Sectigo, etc.
   - Verdict: appropriate once the app reaches a real user base; overkill for now.

2. **Code signing with a standard OV/DV certificate** (partial fix, cheaper)
   - Shows publisher name in the SmartScreen dialog and in UAC prompts.
   - Reputation still builds over time (not instant like EV).
   - Cost: ~$70–200/year. Can use Windows SDK `signtool.exe`.
   - Verdict: reasonable middle ground for a public release.

3. **Self-signed certificate** (no SmartScreen benefit)
   - Does NOT satisfy SmartScreen — reputation is still zero.
   - Only useful for internal/dev use to avoid the "unknown publisher" UAC prompt.
   - Not a solution for end-user distribution.

4. **Distribute via Windows Store / winget** (indirect fix, no cert cost)
   - Apps distributed through MSIX + Partner Center bypass SmartScreen.
   - Requires a Microsoft developer account ($19 one-time).
   - Significant packaging overhead (MSIX format, store review process).
   - Verdict: possible future path, not v0.2.5.

5. **User workaround (current state)**
   - "More info" → "Run anyway" in the SmartScreen dialog.
   - Document this in README for early adopters.

### Decision for v0.2.5
- [x] Document the workaround ("More info" → "Run anyway") in README under Installation.
- [~] Evaluate OV code-signing certificate for v0.3.x public release. ::moved to 0.3.7.md
- [~] Track reputation build-up after first public download link goes live. ::moved to 0.3.7.md

## Fractal of the release
Adjusted Probability Reverse Julia -- **MOTHBALLED to v0.3.7**

The visual output did not match expectations. The formula documentation in the reference source
was too ambiguous to implement confidently. All code was written (orbit accumulation, per-pixel
z_0, 5 parameters) but the fractal has been hidden from the UI and its tests marked `#[ignore]`
until the formula can be clarified. See `plans/v0.3.7.md` for continuation notes.

## True Fractal of the release
The attractors are based upon the following formula for the n+1'th term in the series given 5 parameters a0 a1 ... a4. The integer m is the degree of symmetry. The z is complex valued and all an are real valued. 


z_{n+1} = (a0 + a1*abs(z)^2 + a2*real(z_n^m) + a3*1j)*z_n + a4*conj(z_n)^{m-1}

### Implementation Status

- [x] Core fractal (`core/src/fractals/chaos_symmetry1.rs`): orbit accumulation, 9 tests passing
- [x] CPU f64 path: `accumulate_orbits()` with two-pass burn-in and golden-angle sub-orbit seeds
- [x] CPU Hi-Prec path: `supports_hiprec() = true`, `accumulate_orbits_hiprec()` using f64 orbit
      arithmetic + BigFloat only for screen coordinate mapping (`complex_to_screen_hiprec`)
- [x] GPU f32 path: `chaos_symmetry1_orbit_kernel.wgsl` — `trace_orbit()` with `cs1_mul` and
      `cs1_powi` helpers; registered in `wgpu_backend.rs` via `orbit_pipeline_name_for` and
      `render_orbit_density` parameter mapping (repurposes OrbitParams: `map_re[0..4]` = a0–a4,
      `num_maps` = m)
- [x] GUI: FractalGUI impl with full-width sliders (`spacing.slider_width`), label + TextEdit
      rows for a0–a4, samples (log scale), burn-in, log-density checkbox, seed
- [x] `capability_table.md` updated: all three backends marked yes
- [x] Registered in `mod.rs`, `export.rs`, `cli.rs`, `main.rs`, `app_state.rs`
- [x] `parse_fractal_type` round-trip test passes

The images here are generated by iterating the series typically 1,000 million times. This is performed in two passes, the first pass with many fewer iterations is used to find the bounds of the attractor on the complex plane, the second pass actually "draws" the attractor points. The process of drawing involves treating the bounded region of the complex plane as a 2D histogram, each time the series passes through a pixel region on the plane the histogram at that location is incremented. One might imagine the 2D histogram as a height field, a larger values at a point indicate that the complex series passed through that pixel more often than a point with a smaller value. At the end of the process the histogram is mapped onto colours depending on the histogram values, there are many ways to do this based on aesthetic grounds. 

 A rough guide to parameters.

    - The value of m determines the rotational symmetry of the image, it should be greater than or equal to 2.

    - a0 is usually between -3 and 3, values between -1 and 1 are generally less interesting.

    - a1 should have the opposite sign as a0, and be in the same range.

    - a2 perturbs the attractor, it can be 0.

    - a3 controls bilateral symmetry, 0 will result in mirror symmetry. Otherwise typical values are between -1 and 1.

    - a4 typically between -1 and 1 and not too close to zero. 

## GUI Polish Notes (Known Issues, post-implementation)

- **ChaosSymmetry1 slider precision**: Resolved. Full-width sliders implemented using
  `ui.style_mut().spacing.slider_width = ui.available_width() - 20.0` (save/restore pattern).
  This is the correct egui API; `add_sized()` does NOT control track width. Documented in
  `gui/references/egui_slider_sizing.md`. Step size 0.001, 4 decimal places. TextEdit boxes
  alongside each slider allow precise value entry.

- **Fractal type dropdown text wrapping**: Resolved. ComboBox changed to full-width using
  `.width(ui.available_width() - 4.0)`, placed below a `ui.label("Type:")` rather than inline
  in a `horizontal` layout. No more truncation for long fractal names.

## GUI: Hide Iterations for Orbit Accumulation Fractals

- [x] `FractalTypeOps` trait: added `fn uses_orbit_accumulation(&self) -> bool`
- [x] `impl FractalTypeOps for FractalType` in `app_state.rs`: returns true for
      MultiJuliaIFS, ChaosSymmetry1, AdjProbJulia
- [x] `render_fractal_settings()` in `gui.rs`: entire Iterations block (TextEdit +
      x2/÷2/x10/÷10 buttons + GPU warning) wrapped in `if !fractal_type.uses_orbit_accumulation()`
- [x] Verified: ChaosSymmetry1 and MultiJuliaIFS show no Iterations field; Mandelbrot/Julia
      still show it; switching live works correctly

## Release Checklist

- [x] All plan items complete (see sections above)
- [x] `cargo test --lib` — 165 passed, 0 failed
- [x] GPU test for Cactus — 1/1 passed (RTX 3080 Ti, Vulkan)
- [x] 4K and 8K exports verified
- [x] Version bumped to 0.2.5 in `Cargo.toml` (workspace)
- [x] CHANGELOG.md updated
- [x] README.md updated (v0.2.5 Latest, showcase image, release history entry)
- [x] index.html updated (og:image, twitter:image, showcase section)
- [x] v0.2.4 showcase image moved to `img_resources/gallery_expanded/`
- [x] v0.2.5 showcase: `chaossymmetry1_1280x720_2xGaussian1777355263.png`
- [x] Release zip built: `builds/forma-fractalis_v0.2.5_windows.zip` (5 MB)

